services:
  caddy:
    image: caddy:2-alpine
    environment:
      CADDY_SITE_ADDRESS: ${CADDY_SITE_ADDRESS:-renders.maxaitel.org}
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - ./infra/caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - web
      - api

  api:
    build:
      context: ./backend
    runtime: nvidia
    gpus: all
    user: "${LOCAL_UID:-1000}:${LOCAL_GID:-1000}"
    environment:
      RENDER_STORAGE_ROOT: /data
      BLENDER_BINARY: /usr/local/bin/blender-wrapper
      BLENDER_CYCLES_DEVICE: AUTO
      BLENDER_GPU_ORDER: CUDA,OPTIX,CPU
      ADMIN_BOOTSTRAP_USERNAME: ${ADMIN_BOOTSTRAP_USERNAME}
      ADMIN_BOOTSTRAP_PASSWORD: ${ADMIN_BOOTSTRAP_PASSWORD}
      ADMIN_PANEL_PATH: ${ADMIN_PANEL_PATH:-control-tower}
      AUTH_COOKIE_SECURE: ${AUTH_COOKIE_SECURE:-auto}
      TRUSTED_PROXIES: ${TRUSTED_PROXIES:-127.0.0.1,::1,172.16.0.0/12}
    expose:
      - "8000"
    volumes:
      - ./data:/data
      - ./backend/.blender-build_release_headless:/blender:ro

  web:
    build:
      context: ./frontend
      args:
        BACKEND_ORIGIN: http://api:8000
        NEXT_PUBLIC_ADMIN_PANEL_PATH: ${ADMIN_PANEL_PATH:-control-tower}
    environment:
      BACKEND_ORIGIN: http://api:8000
      NODE_ENV: production
      NEXT_PUBLIC_ADMIN_PANEL_PATH: ${ADMIN_PANEL_PATH:-control-tower}
    expose:
      - "3000"
    depends_on:
      - api

volumes:
  caddy_data:
  caddy_config:
